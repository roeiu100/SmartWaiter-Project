# Used by Expo CLI (see `npm start`). Skips calls to Expo's servers when your network
# blocks them (fixes "TypeError: fetch failed" during "Starting Metro Bundler").
EXPO_OFFLINE=1
EXPO_NO_TELEMETRY=1
EXPO_PUBLIC_API_URL=http://192.168.1.171:3000
